// frontend/src/components/AIGeneratedUI.jsx

import React from 'react';
import SessionHost from './SessionHost';

// This component is now a simple wrapper that renders the mainframe.
const AIGeneratedUI = () => {
  return <SessionHost />;
};

export default AIGeneratedUI;

